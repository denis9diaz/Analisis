<header
  class="bg-blue-600/90 text-white backdrop-blur-md p-4 shadow-lg sticky top-0 z-50"
>
  <div class="container mx-auto flex items-center relative">
    <a href="/" class="flex items-center gap-2">
      <img src="src/public/logo.png" alt="Logo" class="h-16 w-auto" />
    </a>
    <nav class="flex-1 flex justify-center" id="center-nav"></nav>
    <nav class="flex items-center gap-4" id="header-nav"></nav>
  </div>
</header>

<script type="module">
  const centerNav = document.getElementById("center-nav");
  const rightNav = document.getElementById("header-nav");

  const username = localStorage.getItem("username");
  const access = localStorage.getItem("access_token");

  // Función para limpiar sesión
  const clearSession = () => {
    localStorage.removeItem("access_token");
    localStorage.removeItem("refresh_token");
    localStorage.removeItem("username");
  };

  // Verificar validez del access token
  async function checkToken() {
    if (!access) {
      clearSession();
      showGuestLinks();
      return;
    }

    const res = await fetch("http://localhost:8000/api/auth/validate/", {
      headers: {
        Authorization: `Bearer ${access}`,
      },
    });

    if (res.ok) {
      showUserLinks(username);
    } else {
      clearSession();
      showGuestLinks();
    }
  }

  function showUserLinks(username) {
    centerNav.innerHTML = `
      <a href="/analisis" class="px-4 py-2 bg-white/20 hover:bg-white/30 text-white font-semibold rounded-lg transition">
        Análisis
      </a>
    `;

    rightNav.innerHTML = `
      <div class="relative inline-block text-left">
        <button id="user-menu-button" class="font-medium cursor-pointer transition-transform hover:scale-105">
          ${username}
        </button>
        <div id="user-dropdown" class="hidden absolute right-0 mt-2 w-40 bg-white text-black rounded-md shadow-lg z-10 overflow-hidden text-sm">
          <a href="/cuenta" class="block px-4 py-2 hover:bg-gray-100 transition">Mi cuenta</a>
          <div class="border-t border-gray-200"></div>
          <button id="logout-button" class="block w-full text-left px-4 py-2 text-red-600 hover:bg-red-50 hover:text-red-700 transition">
            Logout
          </button>
        </div>
      </div>
    `;

    const userButton = document.getElementById("user-menu-button");
    const dropdown = document.getElementById("user-dropdown");
    const logoutBtn = document.getElementById("logout-button");

    userButton.addEventListener("click", () => {
      dropdown.classList.toggle("hidden");
    });

    logoutBtn.addEventListener("click", () => {
      clearSession();
      window.location.href = "/";
    });

    document.addEventListener("click", (e) => {
      if (!rightNav.contains(e.target)) {
        dropdown.classList.add("hidden");
      }
    });
  }

  function showGuestLinks() {
    centerNav.innerHTML = ``;
    rightNav.innerHTML = `
      <a href="/login" class="bg-white/20 hover:bg-white/30 text-white font-semibold px-4 py-2 rounded-lg transition">
        Iniciar sesión
      </a>
      <a href="/register" class="text-white font-medium">
        Registrarse
      </a>
    `;
  }

  // ✅ Ejecutar al cargar
  checkToken();
</script>
